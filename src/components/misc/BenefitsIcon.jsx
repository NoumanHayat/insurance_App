import React from "react";
import { View, StyleSheet, Image } from "react-native";

const BenefitsIcon = ({ source }) => {

    return (
        <View>
            <Image source={source} />
        </View>
    );
}

export default BenefitsIcon;
